name: Backend CI

on:
  push:
    branches: [main, develop]
    paths:
      - 'app/**'
      - 'pyproject.toml'
      - 'requirements.txt'
      - '.github/workflows/backend-ci.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'app/**'
      - 'pyproject.toml'
      - 'requirements.txt'
      - '.github/workflows/backend-ci.yml'

jobs:
  lint:
    name: Lint Python Code
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          uv sync

      - name: Run ruff linting
        run: |
          uv run ruff check app/

      - name: Run ruff formatting check
        run: |
          uv run ruff format --check app/

  type-check:
    name: Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          uv sync

      - name: Run mypy (if configured)
        run: |
          # Uncomment when mypy is set up
          # uv run mypy app/
          echo "Type checking placeholder - add mypy when ready"

  test:
    name: Run Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install dependencies with dev extras
        run: |
          uv sync --extra dev

      - name: Run pytest
        run: |
          # Uncomment when tests are added
          # uv run pytest
          echo "Test placeholder - add tests when ready"
